# Environment overrides for flext-target-oracle-oic (prefix FLEXT_TARGET_ORACLE_OIC_).
# Copy to .env and fill real values; .env is gitignored — never commit secrets.
FLEXT_TARGET_ORACLE_OIC_ENABLED=true
